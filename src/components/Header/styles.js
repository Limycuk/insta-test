import { makeStyles } from '@material-ui/styles';

export default makeStyles(() => ({
  container: {},
  logo: {
    color: '#ffffff',
    textDecoration: 'none',
  },
  filters: {
    marginLeft: 'auto',
    display: 'flex',
    flexDirection: 'column',
  },
}));
